def remove_num(nums):
    position = 3 - 1
    idx = 0

    n = len(nums)

    while n > 0:
        
        idx = (idx + position) % n
        print(nums.pop(idx))
        n = n-1
        
nums = [1, 2, 3, 4, 5, 6, 7, 8, 9]
print(remove_num(nums))